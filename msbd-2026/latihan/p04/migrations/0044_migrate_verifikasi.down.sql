-- Diminta: pasangan .down.sql untuk 0044.
-- Dipilih: no-op. Migrasi 0044 hanya MEMBACA dan memvalidasi, tidak mengubah
--          skema atau data apa pun, jadi tidak ada state yang perlu
--          dikembalikan.
-- Alternatif: menghapus tidak ada -- tidak relevan, tidak ada alternatif
--          bermakna untuk migrasi yang murni verifikasi.

-- Sengaja kosong: 0044 tidak mengubah state apa pun.
SELECT 1;
